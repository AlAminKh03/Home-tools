import React from 'react';

const Blogs = () => {
    return (
        <div>
            <div>
                <h1>How will you improve the performance of a React Application?</h1>
                <p>There's many way to improve the performace of a react application , Although it is very fast also we can take some steps to make it more faster</p>
            </div>
        </div>
    );
};

export default Blogs;